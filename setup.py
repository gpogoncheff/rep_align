from setuptools import setup, find_packages

setup(
    name='rep_align',
    version='0.0.0',
    author='GP',
    author_email='galenpogoncheff@me.com',
    description='Rep Align Interp',
    packages=find_packages(),
)

# python setup.py sdist bdist_wheel